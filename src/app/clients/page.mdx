import { Libraries } from '@/components/Libraries'

export const metadata = {
  title: 'TofuPilot | Clients',
  description:
    'TofuPilot offers a fine-tuned Python library to make your life easier and give you the best experience when consuming the API.',
}

export const sections = [
  { title: 'Official libraries', id: 'official-library' },
]

# TofuPilot Clients

The recommended way to interact with the TofuPilot API is by using our official client. Today, TofuPilot offers a fine-tuned Python library to make your life easier and give you the best experience when consuming the API. {{ className: 'lead' }}

<Note>
  If you’d like to use a different language than those listed below, or if you
  prefer to work directly with our REST API, feel free to head to the next page.
</Note>

<Libraries />
